名称 = "电视盒子源聚合器" = "tvbox-source-aggregator"
main = "src/cf-entry.ts" = "src/cf-entry.ts"
兼容日期 = "2024-12-30" = "2024-12-30"
兼容性标志 = ["nodejs兼容"] = ["nodejs_compat"]

# 自定义域名（可选，推荐配置）
# 取消注释并替换为你自己的域名和 Zone ID
# 路线 = [
#   { pattern = "tvbox.yourdomain.com/*", zone_id = "你的区域ID" }
# ]

[触发器]
crons = ["0 * * * *"]  # 每小时触发，实际执行频率由 Admin 设置的聚合间隔控制 = ["0 * * * *"]  # 每小时触发，实际执行频率由 Admin 设置的聚合间隔控制

[[kv_namespaces]]
绑定 = "KV" = "KV"
id = "fb9744aff53c422cad20503288df7b3c"
preview_id = "fb9744aff53c422cad20503288df7b3c"

[变量]
SPEED_TIMEOUT_MS = "5000"   # 源地址延迟阈值（毫秒） = "5000"   # 源地址延迟阈值（毫秒）
SITE_TIMEOUT_MS = "3000"    # 站点 API 延迟阈值（毫秒） = "3000"    # 站点 API 延迟阈值（毫秒）
FETCH_TIMEOUT_MS = "5000"   # 抓取配置 JSON 超时（毫秒） = "5000"   # 抓取配置 JSON 超时（毫秒）
WORKER_BASE_URL = "https://tvbox.rio.edu.kg"  # CF 边缘代理依赖此配置（JAR/MacCMS/直播源代理） = "https://tvbox.rio.edu.kg"  # CF 边缘代理依赖此配置（JAR/MacCMS/直播源代理）
